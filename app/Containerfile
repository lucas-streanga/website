FROM node:22-slim

WORKDIR /app

# Install deps first so this layer is cached unless package.json changes
COPY package*.json ./
RUN npm install

# Copy the rest (will be shadowed by the bind mount in dev, but keeps
# the image runnable on its own too)
COPY . .

EXPOSE 4321

CMD ["npm", "run", "dev"]
