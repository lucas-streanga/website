{
  "name": "app",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "node scripts/sync-luna-grammar.mjs",
    "dev": "rm -f .astro/dev.json && astro dev",
    "prebuild": "node scripts/sync-luna-grammar.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "audit": "lighthouse http://localhost:4321 --chrome-flags=\"--headless=new --no-sandbox --disable-dev-shm-usage --disable-gpu\" --output html --output-path ./lighthouse-report.html"  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.0",
    "@fontsource-variable/fraunces": "^5.2.9",
    "@fontsource-variable/inter": "^5.2.8",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "lighthouse": "^13.4.0",
    "shiki": "^4.3.1"
  }
}
